spring:
  config:
    activate:
      on-profile: local
  jpa:
    hibernate:
      ddl-auto: create

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type.descriptor.sql: trace

server:
  port: 8080